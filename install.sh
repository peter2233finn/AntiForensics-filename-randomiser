echo "Copying into /usr/bin and /usr/sbin..."
sudo cp filernd /usr/bin
sudo cp filernd /usr/sbin
echo "Making executible...."
sudo chmod +x /usr/bin/filernd
sudo chmod +x /usr/bin/filernd
echo "All done. Run filernd to use"
